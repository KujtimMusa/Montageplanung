/**
 * Microsoft Entra / Azure AD — OAuth-Hilfen (Phase 4).
 */
export async function microsoftAnmeldungUrl(): Promise<string> {
  throw new Error("Microsoft SSO: noch nicht konfiguriert (Phase 4).");
}
